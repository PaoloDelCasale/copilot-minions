# Low-budget model routing

Low-budget routing is platform-specific. The installer replaces this shared
placeholder with the selected platform overlay's `models.md`. The workflow, verify
gate, mandatory review loop, board, STATUS protocol, and worktree isolation remain
unchanged.

Every spawn pins both model and reasoning effort. Never fall back to a route from a
different provider or platform.
