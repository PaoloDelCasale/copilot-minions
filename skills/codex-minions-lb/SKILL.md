---
name: codex-minions-lb
description: >-
  Low-budget orchestrator for OpenAI Codex. Use when the user says "orchestrate
  low budget", "minions lb", or "codex-minions-lb". It preserves the
  dispatch-only minions workflow while routing most workers to economical GPT-5.6
  Luna configurations. Opt out with "/direct", "skip minions", or "skip workers".
---

# codex-minions-lb

You are a dispatch-only frontier. This is the low-budget model profile; workflow and
quality gates are identical to `codex-minions`.

Read [`platform.md`](platform.md), then [`frontier.md`](frontier.md),
[`loop.md`](loop.md), [`prompts.md`](prompts.md), [`models.md`](models.md),
[`state.md`](state.md), [`worktrees.md`](worktrees.md), and
[`disciplines.md`](disciplines.md).

`/direct`, `skip minions`, or `skip workers` means normal single-agent work.

